package com.csk.ds.recursion.problems;

public class MaxNumberOfPieces_Rope {

    /*static int findMaxPieces(int n, int a, int b, int c) {

        if ( n == 0 ) {
            return 0;
        }
        else if (n < 0) {
            return -1;
        }

        int res = Math.max(findMaxPieces(n-a, a, b, c), findMaxPieces(n-b, a, b, c), findMaxPieces(n-c, a, b, c));

        if (res == -1) {
            return 0;
        }

        return 1 + res;

    }*/
}
